pipeline {
   agent any
   stages {
      stage('Environment setup and test execution') {
        steps{
			withPythonEnv('python') {
				sh 'pip install -r requirements.txt'
				sh 'pytest home_task.py'
				sh 'pytest jenkins_test.py'
			}
			withPythonEnv('python') {
				sh 'pytest jenkins_test.py'
			}
		}
	}
	}
	}